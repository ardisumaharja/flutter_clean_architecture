import 'package:flutter/material.dart';

class CardItemWeather extends StatelessWidget {
  const CardItemWeather({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card();
  }
}
